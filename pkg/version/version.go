package version

// Version is the current version of the TMDB MCP server
const Version = "0.1.0"

// GitCommit is the git commit hash (set during build)
var GitCommit = "unknown"

// BuildDate is the build date (set during build)
var BuildDate = "unknown"
